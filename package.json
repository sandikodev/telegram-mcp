{
  "name": "telegram-mcp",
  "version": "0.1.0",
  "description": "MCP server for Telegram — read history, send messages, manage topics",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "auth": "bun run src/auth.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "telegram": "^2.26.0",
    "zod": "^3.23.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "trustedDependencies": [
    "es5-ext"
  ]
}
